version: '3.8'

services:
  road-guard:
    build: .
    container_name: road-guard-app
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./results:/app/results
    env_file:
      - .env
    restart: unless-stopped
